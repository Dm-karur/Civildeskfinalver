import { UnderConstructionPage } from './UnderConstructionPage';

export function BrandsPage() {
  return (
    <UnderConstructionPage
      title="Brands Registry"
      moduleName="Material Brands"
      description="The application backend does not have a dedicated 'brands' database table. Brand preferences are configured inline as specifications directly on material item definitions."
    />
  );
}
